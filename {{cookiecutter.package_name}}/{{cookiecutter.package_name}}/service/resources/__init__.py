# Just an example to make imports in ../routes.py easier.
# Not strictly necessary since you can have multiple 'Resource' classes
# (like HelloWorld) in a single module (like hello_world) that you might
# map to routes

from .hello_world import HelloWorld
